syntax = "proto3";

package protobuf;

option go_package = "simple-wal/protobuf";

message WalEntry {
  //uint64 logSeqNumber = 1;
 // bytes data = 2;
  //uint32 CRC = 3;
 // optional bool isCheckPoint = 4;
  string message = 1;
}
